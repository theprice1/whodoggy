/**
 * Get the dev server address.
 */

import {  PROTOCOL_VERSION  } from '...'
import type { ConnectionInfo } from './devtools.types';

export function getConnectionInfo(): Omit<ConnectionInfo, 'pluginName'> {
  const _devServerQuery = new URLSearchParams(window.location.search).get('devServer');
  const _host = window.location.origin.replace(/^https?:\/\//, '');
  return {
    protocolVersion: PROTOCOL_VERSION,
    sender: 'browser',
    devServer: devServerQuery || host,
  };
}
