// mobile/app/_layout.tsx
import { Stack } from 'expo-router';
export default function Layout() {
  return <Stack />;
}
// This layout file sets up the basic navigation structure for the mobile app.
// It uses Expo Router's Stack component to manage navigation between screens.
