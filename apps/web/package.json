{
  "name": "whodoggy-web",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "firebase": "^10.0.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "tailwindcss": "^3.3.0",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0",
    "typescript": "^5.0.0"
  }
}
// This is the package.json file for the WhoDoggy web application.
// It defines the project name, version, scripts for development, build, and preview, as well as the dependencies and devDependencies required for the project.
// The dependencies include React, React DOM, and Firebase, while the devDependencies include Vite          