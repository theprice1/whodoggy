module.exports = {
  presets: [
    'babel-preset-expo', // Preset for Expo-based React Native apps
    '@babel/preset-typescript', // TypeScript preset to handle TS files
  ],
  plugins: [
    // Add additional plugins here if necessary
  ],
};
