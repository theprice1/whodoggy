
# 5. Shared Package README.md (`packages/shared/README.md`)

```markdown
# WhoDoggy Shared Package

Contains shared utilities, TypeScript types, and Firebase client/admin helpers used across all packages.

## Usage

- Import common types and utilities to ensure consistency.
- Keep dependencies minimal to avoid bloat.

## Setup

No direct build or run commands. Build handled via root workspace scripts.

## Testing

- Contains unit tests run via Vitest.

---

