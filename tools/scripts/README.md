# WhoDoggy Utility Scripts

This folder contains utility scripts for tasks such as:

- Syncing Firebase and PostgreSQL data
- Seeding mock data
- Cleaning up logs or temp files

## Running Scripts

Use `ts-node` to run scripts directly:

```bash
pnpm --filter whodoggy-tools run start:scripts
